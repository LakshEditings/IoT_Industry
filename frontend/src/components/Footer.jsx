const Footer = () => {
  return (
    <footer className="app-footer">
      <p>&copy; {new Date().getFullYear()} Industrial IoT Dashboard. All credentials strictly encrypted and monitored.</p>
    </footer>
  );
};

export default Footer;
